def patch_rewrite_weak_blocks(full_script: str, acts: dict, scores: dict, fact_spine: dict, tone_preset: str, max_passes: int = 2):
    # TODO: Replace with targeted LLM rewrites only for blocks scoring <=3.
    patch_log = {"passes": 0, "notes": ["stub patch rewrite not implemented yet"]}
    return full_script, patch_log
